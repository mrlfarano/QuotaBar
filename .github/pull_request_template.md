<!-- One source/feature per PR, keep it small -->

## What

<!-- What changes, and why -->

## How I verified

- [ ] `swift build -c release` is clean (no warnings)
- [ ] Offline check added/updated: `--parse-… testdata/…` output pasted below
- [ ] Live check for touched sources: `--probe <source>` output pasted below
      (tokens redacted)
- [ ] Old config still decodes (if config schema changed)

```
<paste probe/parse output here>
```
